# 微信公众号富文本转换工具

一个本地运行的单文件工具，用来把 `HTML` 或 `Markdown` 转成更适合粘贴到微信公众号编辑器的富文本内容。

它支持左侧输入、右侧实时预览，并提供“一键复制富文本到公众号”的能力。工具会自动清洗部分高风险标签，并尽量保留排版效果，适合写公众号、做内容搬运整理、把 Markdown 草稿转进微信后台时使用。

## 功能特点

- 支持 `HTML` 输入
- 支持 `Markdown / MD` 输入
- 自动生成适合公众号粘贴的富文本预览
- 一键复制富文本到剪贴板
- 支持手动全选预览后自行复制
- 支持下载转换后的干净 HTML 文件
- 自动过滤 `script`、`iframe` 等高风险标签
- 可选“尽量内联样式”，提升公众号编辑器的样式保留率
- 纯前端、本地运行、不上传内容

## 适合谁用

- 需要把 Markdown 草稿整理成公众号文章的人
- 习惯先在 HTML / 本地编辑器里排版，再粘贴到微信后台的人
- 希望减少公众号编辑器吞样式、乱格式问题的人

## 使用方式

1. 下载项目中的 `wechat-html-paste-tool.html`
2. 直接用浏览器打开这个文件
3. 在左侧粘贴你的 `HTML` 或 `Markdown`
4. 选择对应输入格式
5. 点击“生成富文本预览”
6. 确认右侧预览效果
7. 点击“复制富文本到公众号”
8. 回到微信公众号编辑器中直接粘贴

如果复制失败，也可以点击“手动全选预览”，然后自行复制粘贴。

## 支持内容

当前工具对常见内容做了友好处理，尤其适合普通图文排版场景，包括：

- 标题
- 段落
- 引用块
- 有序列表
- 无序列表
- 代码块
- 图片
- 基础行内样式

其中 Markdown 会被转换为带内联样式的 HTML 结构，以便更适配微信公众号编辑器。

## 安全说明

为降低粘贴内容里的风险，工具会自动移除或过滤部分内容，例如：

- `script`
- `iframe`
- `object`
- `embed`
- `form`
- `input`
- `button`
- `textarea`
- `select`
- `link`
- `meta`
- `javascript:` 链接
- 某些危险样式表达式

这意味着它更适合做“公众号排版转换工具”，而不是完整保真地还原任意复杂网页。

## 注意事项

- 微信公众号编辑器本身会再次清洗样式，所以无法保证 100% 原样保留
- 复杂布局、外部脚本、交互组件、本地字体等内容通常无法完整保留
- 不同浏览器对剪贴板富文本支持略有差异，建议优先使用较新的 Chrome 或 Edge
- 如果你的内容非常复杂，建议先预览，再少量分段粘贴测试

## 项目特点

这个工具是：

- 单文件 HTML 工具
- 无需安装依赖
- 无需后端服务
- 打开即用
- 适合直接放到 GitHub 作为小工具分享

## 文件说明

- `wechat-html-paste-tool.html`：主工具文件，浏览器直接打开即可使用

## 可能的后续改进

- 支持更多 Markdown 语法
- 提供更丰富的公众号排版模板
- 增强图片、表格、分割线等样式适配
- 增加一键清理多余空行和冗余样式
- 增加深浅主题或自定义排版风格

## License

如果你准备开源，可以按你的需要补充 `MIT`、`Apache-2.0` 或其他许可证说明。

如果你暂时还没决定，也可以先删除这一节，之后再补。
